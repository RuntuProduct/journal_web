import les from './index.less'
import BaseLab from '../baseLab'

const MonthLab = ({
  
}) => {
  return (
    <BaseLab
      className={les.container}
      title="月"
    >month</BaseLab>
  )
}

export default MonthLab
